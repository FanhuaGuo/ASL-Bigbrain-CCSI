The `code/` folder contains the analysis and plotting code.

Running the code in the order of the step labels will produce the article's results. Step 00 involves a relatively complex preprocessing workflow; the preprocessed data has been stored in the `data/` and `reference/` folders. Running the code for steps 01-06 directly will reproduce the article's results.

Detailed Description:
1.
